/*

    var arr: <type>[] = [val1, val2, val3, ...];

*/

let arr: string[] = ['Aryan', 'Kashyap'];
let hetro: any[] = [1, false, 'Aryan'];

let data:  (number | string | boolean)[] = [1, false, 'Aryan'];

let data2: [number, string, boolean] = [1, 'Aryan', false]; // it shapuld be in order  first number than string than boolean as per provided sequence of types